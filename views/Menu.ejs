<!DOCTYPE html>
<html>
    <head>
          <title>Menu</title>
        <style>
            body{background:green; font-family:verdana;
            color:white; padding:150px;}
            h1{font-size:72px; text-transform:uppercase;
            letter-spacing:2px; text-align:center;}
            h2{font-size:40px; text-transform:uppercase;
            letter-spacing:2px; text-align:center;}
            p{font-size:32px; text-align:center;}
        </style>
    </head>
    <body>
        <h1>Welcome to Kedai Mamak!</h1>
        <h2>Menu</h2>
        <ul>
            <% 
                for(i=0;i<menubaru.length;i++)
                { 
                    %>

                        <a href='satupoint/<%=i%>'><h2><%=menubaru[i];%></h2></a>

                    <%
                } 
            %>
        </ul>
    </body>
</html>